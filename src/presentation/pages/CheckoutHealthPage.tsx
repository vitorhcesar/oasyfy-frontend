/** Resposta estática para probe de saúde do domínio de checkout. */
export default function CheckoutHealthPage() {
  return (
    <pre style={{ margin: 0, fontFamily: "monospace", padding: 16 }}>ok</pre>
  );
}
