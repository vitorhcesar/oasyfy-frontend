services:
  frontend:
    image: ghcr.io/vitorhcesar/omegapay-frontend:${APP_VERSION:-latest}
    pull_policy: never
    container_name: omegapay-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
